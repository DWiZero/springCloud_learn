server:
  port: 8762

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: service-demo
  cloud:
    config:
      label: master
      profile: local
      discovery:
        enabled: true
        serviceId: config-server